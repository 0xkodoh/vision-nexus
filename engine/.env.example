# OpenCV Engine & AWS Environment Configuration

# AWS Configuration (Use IAM Roles in production, or credentials in local dev)
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_S3_BUCKET_NAME=opencv-ai-competition-assets

# Service Configuration
ENGINE_HOST=0.0.0.0
ENGINE_PORT=8000
LOG_LEVEL=info
